{
  "version": 2,
  "builds": [
    {
      "src": "todo-server/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "todo-server/$1"
    },
    {
      "src": "/(.*)",
      "dest": "public/$1"
    }
  ]
}